import sparse_causal_model_learner_rl.learners.rl_data
import sparse_causal_model_learner_rl.config

# replay buffer size
Config.buffer_steps = 100000
Config.minibatch_size = 2500

Config.collect_remotely = True

# number of episodes in the queue
Config.future_episode_size = 50

# number of batches to queue
Config.future_batch_size = 50

Config.n_collectors = 15
Config.collect_initial_steps = 100000
Config.min_collected_sample_ratio = 0.9

tune_run.resources_per_trial = {'gpu': 0.4, 'cpu': 17}
